[pytest]
testpaths = tests
markers =
    llm: needs a live provider (Ollama running, or ANTHROPIC_API_KEY set).
          Deselected by default so the suite runs in CI: -m llm to include.
addopts = -q -m "not llm"
